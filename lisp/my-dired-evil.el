;; create the map for dired mode.
;; (setq my-dired-evil//map
;;       `(
;;         (,(kbd "j") #'dired-next-line)
;;         (,(kbd "k") #'dired-previous-line)
;;         )
;;      )

(local-set-key dired-mode-map 
